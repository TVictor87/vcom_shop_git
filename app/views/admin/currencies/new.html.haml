%section.content-header
  %h1
    = t('admin.currencies.title')
    %small= t('helpers.links.new')

%section.content
  .box
    .box-body
      = render 'form'
    .box-footer
      = link_to t('helpers.links.create'), 'javascript:', class: 'btn btn-primary', onclick: "$('.new_currency').submit(); $('body').animate({scrollTop: $('.has-error').offset().top});"