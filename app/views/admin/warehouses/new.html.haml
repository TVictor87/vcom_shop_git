%section.content-header
  %h1
    = t('admin.warehouses.title')
    %small= t('helpers.links.new')

%section.content
  .box
    .box-body
      = render 'form'
    .box-footer
      = link_to t('helpers.links.create'), 'javascript:', class: 'btn btn-primary', onclick: "$('.new_warehouse').submit(); $('body').animate({scrollTop: $('.has-error').offset().top});"
      = link_to 'Back', admin_warehouses_path, class: 'btn btn-primary'
